import {StyleSheet} from "react-native";

export default StyleSheet.create({
  buttonContainer: {
    height: 42,
    paddingHorizontal: 5,
    marginVertical: 5,
    borderRadius: 5,
    alignItems: "center",
    justifyContent: "center",
  },
  loadingSection: {
    flexDirection: "row",
  },
});
